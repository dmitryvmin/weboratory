function getNewEventKey(address: string) {
  return `new-event-${address}`;
}

function getEventKey(address: string) {
  return `new-event-${address}`;
}

export {
  getNewEventKey,
  getEventKey,
};
