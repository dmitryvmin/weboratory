import { TimetableStateType } from "@stores/globalStore/stores/timetable/types";

const timetableInitialState: TimetableStateType = {
  inViewEventsData: undefined,
  timeTable: undefined,
};

export {timetableInitialState};
